# Auto-generated from tools.toml — do not edit manually.
# Regenerate with: python scripts/generate-env.py
#
# Tools requiring manual installation:
#   - genemark: Requires a license from https://topaz.gatech.edu/GeneMark/license_download.cgi
#   - fitild: Build from source: git clone https://github.com/ogotoh/fitild && cd fitild/src && ./configure && make
#   - combinr: Build combinr (cargo build --release) and put target/release/combinr on PATH, or set EUKAN_ASSEMBLE_COMBINR_PATH.
#
# Tools bundled with parent packages (no separate install):
#   - makdbs: bundled with parent package
#   - cdbfasta: bundled with parent package
#   - seqclean: bundled with parent package
#   - jellyfish: bundled with parent package
#   - BuildDatabase: bundled with parent package
#   - gff2gbSmallDNA.pl: bundled with parent package
#   - optimize_augustus.pl: bundled with parent package
#   - bam2wig: bundled with parent package
#   - etraining: bundled with parent package
#   - new_species.pl: bundled with parent package
#   - randomSplit.pl: bundled with parent package
#   - splitMfasta.pl: bundled with parent package
#   - join_aug_pred.pl: bundled with parent package
#   - gtf2gff.pl: bundled with parent package
#   - wig2hints.pl: bundled with parent package
#   - fathom: bundled with parent package
#   - forge: bundled with parent package
#   - hmm-assembler.pl: bundled with parent package
#   - sortgrcd: bundled with parent package
#   - makblk.pl: bundled with parent package
#   - gthconsensus: bundled with parent package
#   - write_EVM_commands.pl: bundled with parent package
#   - recombine_EVM_partial_outputs.pl: bundled with parent package
#   - convert_EVM_outputs_to_GFF3.pl: bundled with parent package
#   - Load_Current_Gene_Annotations.dbi: bundled with parent package
#   - build_comprehensive_transcriptome.dbi: bundled with parent package

name: eukan
channels:
  - bioconda
  - conda-forge
  - defaults

dependencies:
  # Python
  - python>=3.10,<3.13

  # External tools
  - augustus>=3.5
  - codingquarry>=2.0
  - diamond
  - evidencemodeler>=2.1
  - fasta3
  - genomethreader>=1.7
  - gmap>=2024
  - pasa>=2.5
  - repeatmasker>=4.1
  - repeatmodeler>=2.0
  - rmblast
  - samtools>=1.20
  - segemehl>=0.3.4
  - snap
  - spades>=3.15
  - spaln>=3.0
  - star>=2.7
  - table2asn>=1.28
  - trf>=4.09
  - trinity>=2.15
  - ucsc-blat

  # Build dependencies (needed to compile fitild from source)
  - gsl
  - liblbfgs

  # Perl (needed by AUGUSTUS, PASA, EVM, GeneMark helper scripts)
  - perl
  - perl-bioperl
  - perl-dbd-sqlite
  - perl-yaml
  - perl-hash-merge
  - perl-mce
  - perl-parallel-forkmanager
  - perl-math-utils

  # eukan + Python dependencies (installed via pip for version consistency)
  - pip
  - pip:
    - .

